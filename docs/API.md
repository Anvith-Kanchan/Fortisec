# API Reference

The public package root exposes:

```python
from fortisec import (
    identify_hash,
    redact_logs,
    safe_join,
    scan_headers,
    shannon_entropy,
    validate_email,
)
```

Module-level APIs are documented with Google-style docstrings in `src/fortisec`.

